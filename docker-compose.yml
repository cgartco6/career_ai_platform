version: '3.8'
services:
  backend:
    build: ./backend
    ports: ["5000:5000"]
    depends_on: [mongo]
    restart: always  # Self-healing
  mongo:
    image: mongo:latest
    ports: ["27017:27017"]
    volumes:
      - mongodb_data:/data/db
    restart: always
volumes:
  mongodb_data:
