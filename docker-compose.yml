version: '3.8'
services:
  backend:
    build: ./backend
    ports: ["5000:5000"]
    restart: always
  mongo:
    image: mongo:latest
    ports: ["27017:27017"]
    restart: always
volumes:
  mongodb_data:
