{
  "name": "career_ai_platform",
  "version": "1.3.0",
  "scripts": {
    "setup": "./setup.sh",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run selfhealing\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "selfhealing": "cd self-healing && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
